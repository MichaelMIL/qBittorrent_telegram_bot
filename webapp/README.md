# qbit_web — Flutter web app

The browser front end of the torrent butler. Everything about running and
using it is in the repository README (section "Web app").

```bash
flutter test          # widget tests against test/fake_server.dart
flutter build web     # the backend serves build/web
flutter run -d chrome # dev; point the connect screen at the backend URL
```
